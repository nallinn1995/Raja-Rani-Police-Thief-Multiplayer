Created At: 2026-07-28T04:00:09Z
Completed At: 2026-07-28T04:00:12Z
The following changes were made by the multi_replace_file_content tool to: c:\Users\admin\Downloads\project-bolt-sb1-jq5rsrzx\project\src\App.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -23,8 +23,10 @@
 import { AuthOverlay } from "./components/auth/AuthOverlay";
 import { authService, User } from "./services/authService";
 import { UserProfile } from "./components/UserProfile";
-
-const SOCKET_URL = import.meta.env.VITE_SERVER_URL || undefined;
+import { ConnectionModeToggle } from "./components/ConnectionModeToggle";
+import { configService } from "./services/configService";
+
+const SOCKET_URL = configService.getSocketUrl();
 
 const socket = io(SOCKET_URL, {
   transports: ["polling", "websocket"],
@@ -803,6 +803,7 @@
           transition={Bounce}
           theme="dark"
         />
+        <ConnectionModeToggle />
       </div>
 
       {showAuthModal && (
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.